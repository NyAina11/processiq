# Custom certbot image with DuckDNS DNS plugin
# Based on the official certbot image
FROM certbot/certbot:latest

# Install the DuckDNS DNS plugin for certbot
RUN pip install certbot-dns-duckdns --no-cache-dir

ENTRYPOINT []
